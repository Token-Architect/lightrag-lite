VITE_API_PROXY=true
VITE_API_ENDPOINTS=/api,/docs,/redoc,/openapi.json,/static,/login,/auth-status
# Same-origin recommended for all-in-one Docker.
# Set only when frontend and backend are on different origins.
VITE_BACKEND_URL=
