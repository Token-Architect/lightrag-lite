# Frontend development environment (used by `npm run dev-no-bun`)
# Copy to `.env.development` when you need local overrides.

VITE_API_PROXY=true
VITE_API_ENDPOINTS=/api,/docs,/redoc,/openapi.json,/static,/login,/auth-status

# Local backend for development
VITE_BACKEND_URL=http://localhost:9621
